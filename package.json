{
  "name": "meeting-scheduler",
  "version": "1.0.0",
  "description": "Full-stack meeting scheduling platform with real-time invitations and automatic meeting creation using React, Firebase, and Tailwind CSS",
  "author": "Raul Barros",
  "repository": {
    "type": "git",
    "url": "https://github.com/raaulhb/foundermatcha_task.git"
  },
  "scripts": {
    "setup": "npm install && cd frontend && npm install",
    "emulators": "firebase emulators:start --import=./emulator-data --export-on-exit",
    "dev": "concurrently \"npm run emulators\" \"cd frontend && npm run dev\"",
    "frontend": "cd frontend && npm run dev",
    "seed": "node scripts/seed-data.js"
  },
  "keywords": [
    "meeting",
    "scheduler",
    "firebase",
    "react"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "firebase-tools": "^13.0.0",
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
